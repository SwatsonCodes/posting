<!DOCTYPE html>
<html>
<head>
<style>
img {
  padding: 1px;
}
img:hover {
  box-shadow: 0 0 2px 1px rgba(0, 140, 186, 0.5);
}
</style>
</head>
<body>

{{ range . }}
	<br>{{ .Body }}</br>
	{{ range .MediaURLs }}
	<br><a target="_blank" href="{{ . }}">
	  <img src="{{ . }}" style="width:800px">
	</a></br>
	{{ end }}
{{ end }}

</body>
</html>
